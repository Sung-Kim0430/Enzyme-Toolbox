name: Release

# 触发条件：推送 v* 标签时自动发布，手动触发可重跑
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: '要发布的 tag（留空则使用触发本 workflow 的 tag）'
        required: false
        type: string

# 同一 tag 发布只跑一次
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

# 默认最小权限：只读
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # ---------------------------------------------------------------
  # 1. 预构建 preload.so
  # ---------------------------------------------------------------
  build-preload:
    name: Build preload.so (Android NDK)
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout T5M-305-Preload
        uses: actions/checkout@v4.2.2 # sha: 11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Enzymeym/T5M-305-Preload
          ref: 3684f21930cba9a31c0254434059fcad35d9321c  # T5M-305-Preload main @ 2026-08-10
          path: t5m-preload
          fetch-depth: 1

      - name: Install Android NDK
        id: ndk
        uses: nttld/setup-ndk@v1.6.0 # sha: aacaf74664d0f8e1b070a865acb47ee25007386d
        with:
          ndk-version: r29
          add-to-path: false

      - name: Build preload.so
        working-directory: t5m-preload/source
        env:
          NDK_ROOT: ${{ steps.ndk.outputs.ndk-path }}
        run: make preload

      - name: Upload preload.so
        uses: actions/upload-artifact@v4.4.3 # sha: b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882
        with:
          name: preload-so
          path: t5m-preload/source/build/bin/preload.so
          if-no-files-found: error
          retention-days: 7

  # ---------------------------------------------------------------
  # 2. 平台构建矩阵
  # ---------------------------------------------------------------
  build:
    name: Build ${{ matrix.label }}
    needs: build-preload
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Windows x86_64
            runner: windows-latest
            target: x86_64-pc-windows-msvc
          - label: Linux x86_64
            runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
          - label: macOS x86_64
            runner: macos-13
            target: x86_64-apple-darwin
          - label: macOS aarch64
            runner: macos-14
            target: aarch64-apple-darwin

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2 # sha: 11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Install pnpm
        uses: pnpm/action-setup@v4.0.0 # sha: 0c17529a66aca453f9227af23103ed11469b1e47
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4.1.0 # sha: 39370e3970a6d050c480ffad4ff0ed4d3fdee5af
        with:
          node-version: 20
          cache: pnpm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2.7.5 # sha: 5cb072d7354962be830356aa6b146f7612846014
        with:
          workspaces: src-tauri -> target

      - name: Install Linux build deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            file

      - name: Download preload.so
        uses: actions/download-artifact@v4.1.8 # sha: fa0a91b85d4f404e444e00e005971372dc801d16
        with:
          name: preload-so
          path: src/assets/preload

      - name: Rename to expected filename
        run: |
          mkdir -p src/assets/preload
          if [ -f src/assets/preload/preload.so ]; then
            mv src/assets/preload/preload.so \
               src/assets/preload/enzymeym-t5m305-preload-dev.so
          fi
          ls -lh src/assets/preload/

      - name: Install pnpm dependencies
        run: pnpm install --frozen-lockfile

      - name: Type-check & build frontend
        run: pnpm build

      - name: Build Tauri app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: pnpm tauri build --target ${{ matrix.target }}

      - name: Stage bundle artifacts
        env:
          RUNNER_OS: ${{ runner.os }}
        run: |
          mkdir -p out
          if [ "$RUNNER_OS" = "Windows" ]; then
            find src-tauri/target/release -maxdepth 6 \( -name '*.exe' -o -name '*.msi' \) -type f -exec cp {} out/ \;
          else
            find src-tauri/target/release -maxdepth 6 \
              \( -name '*.exe' -o -name '*.dmg' -o -name '*.deb' -o -name '*.AppImage' \
                 -o -name '*.app' -o -name '*.tar.gz' \) \
              -type f -exec cp -r {} out/ \;
            [ -f src-tauri/target/release/t5m-unlock-tool ] && \
              cp src-tauri/target/release/t5m-unlock-tool out/
          fi
          ls -la out/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4.4.3 # sha: b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882
        with:
          name: build-${{ matrix.target }}
          path: out/
          if-no-files-found: error
          retention-days: 14

  # ---------------------------------------------------------------
  # 3. 生成校验和并发布到 GitHub Release
  # ---------------------------------------------------------------
  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    # 只有这个 job 需要写权限
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2 # sha: 11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0

      - name: Download all build artifacts
        uses: actions/download-artifact@v4.1.8 # sha: fa0a91b85d4f404e444e00e005971372dc801d16
        with:
          path: artifacts

      - name: Flatten artifacts & compute SHA-256
        run: |
          mkdir -p release
          for d in artifacts/*/; do
            if [ -d "$d" ]; then
              find "$d" -type f -exec cp {} release/ \;
            fi
          done
          echo "=== Release artifacts ==="
          ls -lh release/
          echo ""
          echo "=== SHA-256 checksums ==="
          cd release
          sha256sum * > SHA256SUMS
          cat SHA256SUMS

      - name: Parse version from input or tag
        id: version
        env:
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          # 优先使用手动输入的 tag，否则用触发本 workflow 的 tag
          if [ -n "$INPUT_TAG" ]; then
            TAG="$INPUT_TAG"
          else
            TAG="${GITHUB_REF_NAME}"
          fi
          # 校验 tag 格式（必须以 v 开头，避免提交垃圾）
          case "$TAG" in
            v*) ;;
            *)
              echo "Error: tag '$TAG' must start with 'v' (e.g. v0.1.0)" >&2
              exit 1
              ;;
          esac
          VER="${TAG#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Resolved version: $VER (tag: $TAG)"

      - name: Generate release notes
        env:
          VERSION: ${{ steps.version.outputs.version }}
          REPO: ${{ github.repository }}
        run: |
          cat > release-notes.md <<EOF
          # Enzyme Toolbox v${VERSION}

          ## 平台产物

          | 平台 | 架构 | 包含文件 |
          |------|------|---------|
          | Windows | x86_64 | NSIS 安装包、可便携 exe |
          | Linux | x86_64 | .deb、AppImage |
          | macOS | x86_64 | .app、.dmg |
          | macOS | aarch64 (Apple Silicon) | .app、.dmg |

          ## 校验

          所有产物的 SHA-256 校验和见 \`SHA256SUMS\` 文件。

          ## 使用说明

          - **Windows**: 双击 NSIS 安装包进行安装；或使用便携版直接运行
          - **Linux**: \`sudo dpkg -i *.deb\` 安装 deb；或直接赋予 AppImage 执行权限并运行
          - **macOS**: 首次启动请在「系统设置 → 隐私与安全性」中允许（未签名）。

          完整文档请见 [README](https://github.com/${REPO}#readme)。
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.0.5 # sha: 4fabdc8bec0ad32a2eb09fb8fd1d04fd30a75562
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Enzyme Toolbox v${{ steps.version.outputs.version }}
          body_path: release-notes.md
          draft: false
          prerelease: ${{ contains(steps.version.outputs.tag, '-') }}
          fail_on_unmatched_files: true
          files: |
            release/*
          overwrite_files: true
